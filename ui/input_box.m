classdef input_box < handle & menu & text_box
properties
    target
    edited but non-applied color
    name % based on target

    CURSOR
end
methods
    init;
        gen name (based on target);
    get from meta;
        (meta.stm.type.bShow);
        format
        limits;
        inc;
        show;
        m_target;

    gen keys_dict;
        m_target;
        i_updates_group;
            i_update_group;
    get info frm var;
    keys_dict -> key;
    run_m_target;
    get value from target;
    draw;
    check string <- cursor;
    set value -> target;
end
